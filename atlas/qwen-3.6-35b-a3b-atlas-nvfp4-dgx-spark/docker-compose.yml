# Atlas inference setup for Qwen3.6-35B-A3B NVFP4 on DGX Spark (GB10)
#
# Reference post by Azeez (Atlas team):
#   https://x.com/AtlasInference/status/2055740933057007745?s=20
# Atlas GitHub: https://github.com/Avarok-Cybersecurity/atlas

services:
  qwen-atlas-nvfp4:
    image: avarok/atlas-gb10:latest
    container_name: qwen-3-6-35b-a3b-nvfp4-atlas
    hostname: inference-server
    platform: linux/arm64

    ports:
      - "8000:8000"

    volumes:
      - ~/.cache/huggingface:/root/.cache/huggingface
      - /dev/shm:/dev/shm

    shm_size: "32g"
    ipc: host

    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]

    environment:
      HF_HUB_ENABLE_HF_TRANSFER: "1"

    command:
      # Serve the Qwen3.6-35B-A3B NVFP4 model via Atlas inference engine
      # Vanilla config from: https://x.com/AtlasInference/status/2055740933057007745?s=20
      - "serve"
      - "${ATLAS_MODEL_ID:-RedHatAI/Qwen3.6-35B-A3B-NVFP4}"
      - "--model-name"
      - "${ATLAS_MODEL_NAME:-Qwen3.6-35B-A3B-NVFP4}"

      # --- API & NETWORKING ---
      # --bind 0.0.0.0: Atlas listens on ALL interfaces inside the container.
      #   This is REQUIRED because the model runs in a Docker container, so clients
      #   do NOT reach it via 127.0.0.1 like they would on a local gaming GPU.
      #   The nginx reverse proxy (nginx-proxy) forwards requests here over the
      #   shared Docker network using the hostname "inference-server" set above.
      # SECURITY NOTE: --bind only controls which NIC Atlas binds to inside the
      #   container, NOT which hosts can reach it. To restrict external access,
      #   remove the port mapping below so port 8000 is only reachable from other
      #   containers on the Docker network (i.e., nginx-proxy). With port mapping
      #   present, any host that can reach this machine's IP on port 8000 will
      #   connect directly — rely on --require-auth / --auth-token as gate.
      - "--bind"
      - "0.0.0.0"
      - "--port"
      - "8000"

      # Atlas uses --auth-token (single bearer token). Token leaks via ps but
      # that's fine for dev — use --auth-tokens-file in production.
      # Value comes from ATLAS_API_KEY in .env (gitignored, see .env.example).
      - "--require-auth"
      - "--auth-token"
      - "${ATLAS_API_KEY:-dummy-key}"

      # --- KV CACHE & QUANTIZATION ---
      # --kv-cache-dtype fp8: Switching to FP8 E4M3 for KV cache as a last attempt to
      #   improve output quality. The NVFP4 setting was faster but appeared to contribute
      #   to doom loops and incorrect tool calls observed on this model. FP8 is the safe
      #   default per Atlas docs (cli.rs:60-63) and may produce more reliable outputs at
      #   the cost of some throughput. If quality improves, this could be the right trade-off.
      - "--kv-cache-dtype"
      - "fp8"

      # --kv-high-precision-layers auto: Keeps first 2 + last 2 attention layers in
      #   BF16 KV cache precision, protecting attention sink tokens and output quality
      #   from quantization error while saving memory on middle layers. Without this
      #   (default=0), Atlas warns: "NVFP4 models may hallucinate or lose coherence
      #   at long context." The small overhead of 4 BF16 layers is worth the quality gain.
      #   Reference: atlas/crates/spark-server/src/cli.rs:65-71 —
      #   "Accepts: number, 'auto' (=2, recommended), 'max'/'all' (all BF16)."
      # DISABLED 2026-05-18: Switching to pure NVFP4 across all layers to maximize
      #   KV cache capacity. With --gpu-memory-utilization 0.70 and reduced prefill
      #   arena, the freed memory goes to more KV blocks for better concurrency.
      #   Watch for coherence degradation on very long outputs (>32K tokens). Re-enable
      #   if hallucination or quality issues appear.
      # - "--kv-high-precision-layers"
      # - "auto"

      # --gpu-memory-utilization: Fraction of GPU VRAM for model + KV cache.
      #   0.75 = 75% of GB10 memory (~96GB). Running single model, no concurrency contention,
      #   so we give it ample headroom for weights + KV cache + MTP speculator overhead.
      - "--gpu-memory-utilization"
      - "0.75"

      # --max-seq-len: Maximum sequence length in tokens.
      #   Qwen3.6-35B-A3B has native context of 262,144 (~256K), extensible to ~1M.
      #   Set to 200K for comfortable headroom within the trained range.
      - "--max-seq-len"
      - "200000"

      # --- CHUNKED PREFILL ---
      # --max-prefill-tokens: Maximum tokens processed per prefill iteration.
      #   16384 covers typical prompts (~14K tokens) in a single pass without wasting
      #   buffer arena memory. At 65536, the buffer arena ballooned to ~12 GB (from
      #   ~1.5 GB at default), starving decode and reducing tok/s from ~60 to ~50.
      #   16384 keeps the arena around ~3 GB, freeing memory for more KV cache blocks.
      #   Default is 8192; this 2x increase still improves TTFT on long prompts while
      #   preserving decode speed.
      - "--max-prefill-tokens"
      - "16384"

      # --- SPECULATIVE DECODING — RE-ENABLED 2026-05-19 ---
      # Despite ~96% K2 rejection rate, MTP speculation delivers real speedup:
      # - With spec ON (NVFP4): 56–72 tok/s across various output lengths
      # - With spec ON (FP8): 70–80 tok/s (slightly faster but less efficient per cli.rs)
      # - With spec OFF (today's baseline): 51.8–52.9 tok/s
      # The cheap K1 draft is accepted enough to amortize the verify cost, netting ~20-30% gain.
      # NVFP4 chosen over FP8 per cli.rs:150-153 (fused device-side expert dispatch vs D2H sync).
      - "--mtp-quantization"
      - "nvfp4"
      - "--num-drafts"
      - "1"
      - "--speculative"

      # --- SCHEDULING & TOOLS ---
      # --scheduling-policy slai: Shared-latency-aware inference scheduling.
      # --tool-call-parser qwen3_coder: Required for tool calling with Qwen3 models.
      # --enable-prefix-caching: Reuse KV cache across requests with common prefixes.
      - "--scheduling-policy"
      - "slai"
      - "--tool-call-parser"
      - "qwen3_coder"
      - "--enable-prefix-caching"

    networks:
      - development-network

networks:
  development-network:
    external: true
