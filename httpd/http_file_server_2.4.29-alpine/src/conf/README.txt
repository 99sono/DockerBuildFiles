This folder contains configuration files.
It should be copied over to the docker machine into:
/usr/local/apache2/conf

The conf dir was originally taken from the apache2 httpd machine, and it is being done to do what we need.