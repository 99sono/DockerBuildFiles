# metadata

This directory contains dumped docker container logs from inference runs.

Each file is named `<timestamp>_<container-name>_log_dump.txt`.

The `05_b_dump_logs.sh` script in `head/` and `worker/` writes here.
