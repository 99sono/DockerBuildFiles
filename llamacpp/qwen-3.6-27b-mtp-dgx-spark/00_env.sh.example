#!/bin/bash
# =============================================================================
# 00_env.sh.example
# =============================================================================
# Copy this file to .env and edit with your actual values.
# Do NOT commit the real .env or 00_env.sh files - they contain secrets.
#
# Usage:
#   cp 00_env.sh.example .env
#   # Edit .env with your values
#   source 00_env.sh
# =============================================================================

set -euo pipefail

# Load from .env if it exists
if [ -f .env ]; then
    export $(grep -v '^#' .env | xargs)
fi

# Default values (can be overridden by .env)
# Replace with your actual llama.cpp server API key
export LLAMA_API_KEY="${LLAMA_API_KEY:-dummy-key}"
